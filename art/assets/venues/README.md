# McCall-Hart Venue Assets

This folder will hold approved reusable background plates for recurring fictional locations.

`scripts/generate_remaining_venue_backgrounds.py` creates only a planning inventory. It does not call an image model. Production remains on hold until the script and comic-production flags are approved.

