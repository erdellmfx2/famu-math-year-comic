# Active V2 Assets

This directory is reserved for approved McCall-Hart character and reusable visual assets.

The v1 character and classroom references are isolated under `assets/archive/`. Do not create or add v2 images until both approval flags in `story/approval_status.json` are true.

