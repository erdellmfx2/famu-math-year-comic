# V1 Asset Archive

These character and classroom images belong to the legacy visual system. They are retained for provenance but must not be supplied to an image model for v2.

Approved v2 character turnarounds and reusable settings will eventually live in the active `assets/` and `art/assets/` directories after the script gate opens.

